<Window x:Class="Test.异步测试"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="SyncTest" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
           <Label Margin="20" Height="100" Width="100" x:Name="Label1">
               <TextBlock x:Name="任务1" Text="任务一"  FontSize="15"></TextBlock>
           </Label>
           <Label Margin="20" Height="100" Width="100" x:Name="Label2">
               <TextBlock x:Name="任务2" Text="任务二"  FontSize="15"></TextBlock>
           </Label>
           <Label Margin="20" Height="100" Width="100" x:Name="Label3">
               <TextBlock x:Name="任务3" Text="任务三"  FontSize="15"></TextBlock>
           </Label>
        </StackPanel>
        
        <StackPanel Grid.Row="1">
            <Button Background="DeepSkyBlue" Click="Button_OnClick"  HorizontalAlignment="Center" Width="100" Height="50">Start</Button>
            <TextBlock Text="" x:Name="text" HorizontalAlignment="Center" Foreground="Red" Margin="0,20,0,0"></TextBlock>
        </StackPanel>
       
    </Grid>
</Window>
