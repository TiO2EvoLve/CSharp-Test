<Window x:Class="Test.图像处理测试"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ImageTest" Height="450" Width="800">
    <Grid>
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10" Orientation="Horizontal">
                <Button Content="选择图像" Width="75" Click="SelectImageButton_Click" />
                <Button Content="添加水印" Width="75" Click="AddWatermarkButton_Click" />
                <Button Click="SaveImageButton_Click">保存图片</Button>
            </StackPanel>
            <Border Height="300" Width="300" HorizontalAlignment="Center" Margin="10" BorderThickness="1"
                    BorderBrush="Gray">
                <Image x:Name="DisplayImage" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</Window>