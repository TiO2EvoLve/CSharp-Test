<Window x:Class="Test.数据库测试"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="DataBase" Height="800" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Style="{StaticResource ButtonPrimary}" Click="ButtonBase_OnClick" VerticalAlignment="Top" Margin="0,10,0,0">查询数据库</Button>
        <StackPanel Grid.Row="1" Margin="20" >
            <DataGrid Background="LightGray" x:Name="UsersDataGrid" AutoGenerateColumns="True"/>
        </StackPanel>
        
    </Grid>
</Window>
